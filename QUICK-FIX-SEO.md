# ⚡ Быстрое исправление SEO для Google Rich Results

## 🎯 Проблема
Google не видит JSON-LD схемы на https://edahouse.ordis.co.il/ потому что PM2 запущен без `NODE_ENV=production`.

## ✅ Решение (5 минут)

### На VPS выполните эти команды:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/edahouse

# 2. Проверьте текущий NODE_ENV
pm2 env edahouse | grep NODE_ENV
# Если не показывает NODE_ENV=production - нужно исправить!

# 3. Остановите и удалите текущий процесс
pm2 stop edahouse
pm2 delete edahouse

# 4. Запустите с NODE_ENV=production
pm2 start dist/index.js --name edahouse -i 1 --env production

# 5. Сохраните конфигурацию
pm2 save

# 6. Проверьте что NODE_ENV установлен
pm2 env edahouse | grep NODE_ENV
# Должно показать: NODE_ENV=production ✅
```

### Проверка что работает:

```bash
# На VPS выполните:
curl -A "Googlebot" -H "Accept: text/html" http://localhost:5000/ 2>/dev/null | grep '"@type"'

# Должны увидеть:
# "@type": "Restaurant"
# "@type": "ItemList"
# Значит всё работает! ✅
```

**ВАЖНО**: Флаг `-H "Accept: text/html"` обязателен! Middleware проверяет этот заголовок. Настоящий Googlebot всегда его отправляет.

### Тест в Google Rich Results:

1. Откройте: https://search.google.com/test/rich-results  
2. Введите: `https://edahouse.ordis.co.il/`  
3. Нажмите "Test URL"  
4. Должны увидеть зелёные галочки для:
   - ✅ Restaurant  
   - ✅ ItemList (категории)  
   - ✅ ItemList (продукты)  

---

## 📝 Для будущих деплоев

### Вариант A: Используйте ecosystem.config.js (рекомендуется)

```bash
# 1. Скопируйте ecosystem.config.js на VPS в /var/www/edahouse/

# 2. Запустите PM2:
cd /var/www/edahouse
pm2 start ecosystem.config.js --env production

# 3. Сохраните:
pm2 save
```

### Вариант B: Всегда указывайте --env production

```bash
# При каждом перезапуске:
pm2 restart edahouse --env production --update-env
```

---

## 🎉 Готово!

Теперь Google увидит:
- Название ресторана
- Адрес и телефон
- Часы работы
- Список категорий (для sitelinks)
- Специальные предложения

Это улучшит SEO и увеличит кликабельность в поисковой выдаче! 🚀
